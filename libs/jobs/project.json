{
  "name": "jobs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/jobs/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project jobs --web",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["trigger dev -c ./libs/jobs/trigger.config.ts"]
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["trigger deploy -c ./libs/jobs/trigger.config.ts"]
      }
    }
  }
}
